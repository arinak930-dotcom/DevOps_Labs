apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-deployment
  namespace: lab
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hello
  template:
    metadata:
      labels:
        app: hello
    spec:
      containers:
        - name: hello
          image: hello-k8s:v1
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5000
